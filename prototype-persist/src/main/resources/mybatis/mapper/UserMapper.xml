<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.inesafujitsu.prototype.persist.mapper.UserMapper">
    <resultMap id="result_map" type="User">
        <id column="id" property="id" javaType="Long"/>
        <result column="name" property="name" javaType="String"/>
        <result column="age" property="age" javaType="Integer"/>
        <result column="email" property="email" javaType="String"/>
        <result column="gender" property="gender" javaType="Integer"/>
    </resultMap>

    <sql id="column_list">
        id, name, age, email, gender
    </sql>

    <select id="getAll" resultMap="result_map">
        SELECT
        <include refid="column_list"/>
        FROM user
    </select>

    <select id="getOne" parameterType="Long" resultMap="result_map">
        SELECT
        <include refid="column_list"/>
        FROM user
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="User">
       INSERT INTO user (name,age,email)
       	VALUES (#{name}, #{age}, #{email})
    </insert>

    <update id="update" parameterType="User">
        UPDATE user SET
        <if test="name != null">name = #{name},</if>
        <if test="age != null">age = #{age},</if>
        nick_name = #{nickName}
        WHERE
        id = #{id}
    </update>

    <delete id="delete" parameterType="Long">
       DELETE FROM user WHERE id = #{id}
    </delete>

</mapper>